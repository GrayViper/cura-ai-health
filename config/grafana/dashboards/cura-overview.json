{
  "annotations": { "list": [] },
  "editable": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "cura-prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "HTTP request rate",
      "type": "timeseries",
      "targets": [
        { "expr": "rate(cura_http_requests_total[5m])", "legendFormat": "requests/sec", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "cura-prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Application uptime",
      "type": "timeseries",
      "targets": [
        { "expr": "cura_process_uptime_seconds", "legendFormat": "uptime", "refId": "A" }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["cura", "operations"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Cura Application Overview",
  "version": 1
}